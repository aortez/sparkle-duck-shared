[Unit]
Description=Set hostname from /boot/hostname.txt
DefaultDependencies=no
After=local-fs.target
Before=sysinit.target

[Service]
Type=oneshot
ExecStart=/usr/sbin/hostname-setup
RemainAfterExit=yes

[Install]
WantedBy=sysinit.target
